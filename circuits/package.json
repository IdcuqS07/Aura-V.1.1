{
  "name": "aura-zk-circuits",
  "version": "1.0.0",
  "description": "Zero-Knowledge Proof circuits for Aura Protocol",
  "scripts": {
    "compile": "circom poh.circom --r1cs --wasm --sym --c",
    "setup": "snarkjs groth16 setup poh.r1cs pot12_final.ptau poh_0000.zkey",
    "contribute": "snarkjs zkey contribute poh_0000.zkey poh_final.zkey --name='First contribution'",
    "export-vkey": "snarkjs zkey export verificationkey poh_final.zkey verification_key.json",
    "export-solidity": "snarkjs zkey export solidityverifier poh_final.zkey ../contracts/contracts/Verifier.sol",
    "test": "node test_circuit.js"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.0"
  },
  "devDependencies": {
    "circom": "^2.1.6"
  }
}
